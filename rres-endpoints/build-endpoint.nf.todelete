#!/usr/bin/env nextflow

params.params_id = 'aratiny-1'

process addUris
{
	output:
		path "knowledge-graph-uris.oxl" into uri_oxl_ch
		
	"""
	"$MY_HOME/add-uris.sh" "${params.params_id}"
	# Trick to have files out of the working dir
	ln -s "$CFG_DATASET_TARGET/knowledge-graph-uris.oxl" "knowledge-graph-uris.oxl"
	"""
}